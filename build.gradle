plugins {
    id 'base'
}

apply from: 'gradle/platform-versions.gradle'

tasks.named('build') {
    dependsOn subprojects.collect { "${it.path}:build" }
}

tasks.named('clean') {
    dependsOn subprojects.collect { "${it.path}:clean" }
}
